import { Typography } from "@mui/material";
import React from "react";

const Dashboard = () => {
  return (
    <div>
      <Typography variant="primaryTitle">Admin page </Typography>
    </div>
  );
};

export default Dashboard;
